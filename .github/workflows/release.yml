name: Release

on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: Release tag to build and publish, for example v0.1.0
        required: true
        type: string
      draft:
        description: Publish as draft release
        required: false
        default: false
        type: boolean
      build_flavor:
        description: "direct = NSIS + Tauri updater; store = MSIX for Microsoft Store (disables updater)"
        required: false
        default: direct
        type: choice
        options:
          - direct
          - store
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  APP_SLUG: aether
  APP_PRODUCT_NAME: Aether
  DESKTOP_DIR: .
  NODE_VERSION: 22
  NEXUSTOOLS_API_URL: https://pbithxqiu7.execute-api.us-east-2.amazonaws.com/dev
  BUILD_FLAVOR: ${{ github.event_name == 'workflow_dispatch' && inputs.build_flavor || 'direct' }}
  RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag_name || github.ref_name }}
  AETHER_UPDATER_ENDPOINT: https://pbithxqiu7.execute-api.us-east-2.amazonaws.com/dev/api/apps/aether/updates/latest?platform=windows-x64

jobs:
  build:
    name: Build Windows (x64)
    runs-on: windows-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ env.RELEASE_TAG }}

      - name: Checkout shared SDK
        uses: actions/checkout@v4
        with:
          repository: xtc002007/NexusTools-shared
          token: ${{ secrets.PAT_TOKEN }}
          path: NexusTools-shared-temp

      - name: Move shared SDK to sibling path
        shell: bash
        run: |
          mv NexusTools-shared-temp ../NexusTools-shared

      - name: Checkout release scripts
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          path: .release-scripts

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Validate release version
        shell: bash
        run: |
          set -euo pipefail

          node <<'NODE'
          const fs = require("fs");
          const packageJson = JSON.parse(fs.readFileSync("package.json", "utf8"));
          const tauriConfig = JSON.parse(fs.readFileSync("src-tauri/tauri.conf.json", "utf8"));
          const cargoToml = fs.readFileSync("src-tauri/Cargo.toml", "utf8");
          const cargoVersion = cargoToml.match(/^version\s*=\s*"([^"]+)"/m)?.[1];
          const tagName = process.env.RELEASE_TAG;
          const expectedTag = `v${packageJson.version}`;

          if (tagName !== expectedTag) {
            throw new Error(`Release tag ${tagName} does not match package version ${expectedTag}.`);
          }
          if (tauriConfig.version !== packageJson.version) {
            throw new Error(`Tauri version ${tauriConfig.version} does not match package version ${packageJson.version}.`);
          }
          if (cargoVersion !== packageJson.version) {
            throw new Error(`Cargo version ${cargoVersion} does not match package version ${packageJson.version}.`);
          }
          NODE

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Restore Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target
          cache-bin: false

      - name: Build shared SDK
        shell: bash
        run: |
          cd ../NexusTools-shared/nexustools-sdk
          npm install
          npm run build

      - name: Install frontend dependencies
        shell: bash
        run: |
          npm install --no-audit --no-fund
          rm -rf node_modules/nexustools-sdk
          cp -r ../NexusTools-shared/nexustools-sdk node_modules/nexustools-sdk
          cd node_modules/nexustools-sdk
          npm install --no-audit --no-fund
          npm run build
          test -f dist/device.js || (echo "SDK missing dist/device.js — push latest NexusTools-shared" && exit 1)
          grep -q 'setDeviceIdProvider' dist/index.js || (echo "SDK missing setDeviceIdProvider export" && exit 1)
          grep -q 'createTauriDeviceIdProvider' dist/device.js || (echo "SDK missing createTauriDeviceIdProvider export" && exit 1)
          echo "SDK exports OK"

      - name: Prepare updater config
        shell: bash
        env:
          AETHER_SIGNING_PRIVATE_KEY: ${{ secrets.AETHER_SIGNING_PRIVATE_KEY }}
          AETHER_UPDATER_PUBLIC_KEY: ${{ secrets.AETHER_UPDATER_PUBLIC_KEY }}
          BUILD_FLAVOR: ${{ env.BUILD_FLAVOR }}
        run: |
          set -euo pipefail

          node <<'NODE'
          const fs = require("fs");
          const flavor = process.env.BUILD_FLAVOR || "direct";

          if (flavor === "store") {
            const storeConfig = {
              bundle: {
                createUpdaterArtifacts: false,
                targets: ["msi"],
              },
            };
            fs.writeFileSync("tauri.store.conf.json", `${JSON.stringify(storeConfig, null, 2)}\n`);
            console.log("Store flavor: updater disabled, MSI-only bundle.");
            process.exit(0);
          }

          const endpoint = process.env.AETHER_UPDATER_ENDPOINT;
          const privateKey = process.env.AETHER_SIGNING_PRIVATE_KEY?.trim();
          const publicKey = process.env.AETHER_UPDATER_PUBLIC_KEY?.trim();

          if (!privateKey) {
            throw new Error("Missing AETHER_SIGNING_PRIVATE_KEY repository secret for signed updater artifacts.");
          }
          if (!publicKey) {
            throw new Error("Missing AETHER_UPDATER_PUBLIC_KEY repository secret for updater verification.");
          }

          const updaterConfig = {
            bundle: {
              createUpdaterArtifacts: true,
              targets: ["nsis"],
            },
            plugins: {
              updater: {
                endpoints: [endpoint],
                pubkey: publicKey,
                windows: {
                  installMode: "passive"
                }
              }
            }
          };

          fs.writeFileSync("tauri.updater.conf.json", `${JSON.stringify(updaterConfig, null, 2)}\n`);
          NODE

      - name: Resolve Tauri build args
        id: tauri_args
        shell: bash
        env:
          ASSET_PLATFORM: windows
          RELEASE_TAG: ${{ env.RELEASE_TAG }}
          BUILD_FLAVOR: ${{ env.BUILD_FLAVOR }}
          TAURI_BUILD_ARGS: --target x86_64-pc-windows-msvc
        run: |
          set -euo pipefail
          tauri_args="$(node .release-scripts/scripts/release/resolve-tauri-build-args.mjs)"
          if [[ "${BUILD_FLAVOR}" == "store" ]]; then
            tauri_args="${tauri_args} --config tauri.store.conf.json"
          else
            tauri_args="${tauri_args} --config tauri.updater.conf.json"
          fi
          echo "args=${tauri_args}" >> "${GITHUB_OUTPUT}"

      - name: Build and bundle app
        id: tauri_build
        uses: tauri-apps/tauri-action@v0.6.0
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.AETHER_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.AETHER_SIGNING_PRIVATE_KEY_PASSWORD }}
          VITE_NEXUSTOOLS_API_URL: https://pbithxqiu7.execute-api.us-east-2.amazonaws.com/dev
          VITE_NEXUSTOOLS_WEBSITE_URL: https://nexustools.tech
        with:
          args: ${{ steps.tauri_args.outputs.args }}

      - name: Normalize release asset names
        shell: bash
        env:
          ASSET_ARCH: x64
          ASSET_PLATFORM: windows
          RELEASE_VERSION: ${{ env.RELEASE_TAG }}
          TARGET: x86_64-pc-windows-msvc
        run: node .release-scripts/scripts/release/normalize-release-artifacts.mjs

      - name: Collect bundle paths
        id: bundle_paths
        shell: bash
        env:
          ARTIFACT_PATHS_JSON: ${{ steps.tauri_build.outputs.artifactPaths }}
        run: |
          set -euo pipefail

          node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const artifactPathsJson = process.env.ARTIFACT_PATHS_JSON || "[]";
          const bundleRoot = path.join("src-tauri", "target", "x86_64-pc-windows-msvc", "release", "bundle");
          const artifactPaths = [];
          const seen = new Set();

          function addArtifact(filePath) {
            if (!filePath) return;
            if (filePath.endsWith("/latest.json") || filePath.endsWith("\\latest.json")) return;

            let artifactPath = filePath;
            if (!path.isAbsolute(artifactPath) && !fs.existsSync(artifactPath)) {
              artifactPath = path.join(".", artifactPath);
            }

            try {
              if (!fs.statSync(artifactPath).isFile()) return;
              artifactPath = fs.realpathSync(artifactPath);
            } catch {
              return;
            }

            if (!seen.has(artifactPath)) {
              seen.add(artifactPath);
              artifactPaths.push(artifactPath);
            }
          }

          function collectBundleFiles(root) {
            if (!fs.existsSync(root)) return;

            const stack = [root];
            const releaseFileSuffixes = [
              ".app.tar.gz", ".AppImage", ".deb", ".dmg", ".exe", ".msi", ".rpm",
              ".sig", ".tar.gz", ".zip",
            ];

            function isReleaseFile(fileName) {
              return releaseFileSuffixes.some((suffix) => fileName.endsWith(suffix));
            }

            while (stack.length > 0) {
              const current = stack.pop();
              const entries = fs.readdirSync(current, { withFileTypes: true });

              for (const entry of entries) {
                const entryPath = path.join(current, entry.name);
                if (entry.isDirectory()) { stack.push(entryPath); continue; }
                if (isReleaseFile(entry.name)) addArtifact(entryPath);
              }
            }
          }

          try {
            const parsed = JSON.parse(artifactPathsJson);
            if (Array.isArray(parsed)) {
              for (const candidate of parsed) {
                if (typeof candidate === "string") addArtifact(candidate);
              }
            }
          } catch {
            // Fall back to walking the Tauri bundle directory
          }

          collectBundleFiles(bundleRoot);

          if (artifactPaths.length === 0) {
            throw new Error("No release bundle artifacts were found.");
          }

          artifactPaths.sort();
          fs.appendFileSync(process.env.GITHUB_OUTPUT, `paths<<__PATHS__\n${artifactPaths.join("\n")}\n__PATHS__\n`);
          NODE

      - name: Create updater metadata
        if: env.BUILD_FLAVOR == 'direct'
        shell: bash
        env:
          ARTIFACT_PATHS_RAW: ${{ steps.bundle_paths.outputs.paths }}
          UPDATER_PLATFORM: windows-x86_64
        run: node .release-scripts/scripts/release/create-updater-metadata.mjs

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.APP_SLUG }}-windows-x64-bundles
          path: |
            ${{ steps.bundle_paths.outputs.paths }}
            release-metadata.json
          if-no-files-found: error

  publish_release:
    name: Publish GitHub Release
    if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.RELEASE_TAG }}

      - name: Checkout release scripts
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          path: .release-scripts

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Determine changelog range
        id: changelog_range
        shell: bash
        run: |
          set -euo pipefail

          previous_tag="$(git describe --tags --abbrev=0 "HEAD^" 2>/dev/null || true)"

          if [[ -n "${previous_tag}" ]]; then
            echo "from_ref=${previous_tag}" >> "${GITHUB_OUTPUT}"
          else
            first_commit="$(git rev-list --max-parents=0 HEAD | tail -n 1)"
            echo "from_ref=${first_commit}" >> "${GITHUB_OUTPUT}"
          fi

          echo "to_ref=${RELEASE_TAG}" >> "${GITHUB_OUTPUT}"

      - name: Generate release notes
        shell: bash
        run: |
          set -euo pipefail

          npx changelogen@latest \
            --from "${{ steps.changelog_range.outputs.from_ref }}" \
            --to "${{ steps.changelog_range.outputs.to_ref }}" \
            --no-output \
            > release-notes.md 2>/dev/null || true

          if [[ ! -s release-notes.md ]]; then
            {
              echo "## Changes"
              echo
              echo "- Release ${RELEASE_TAG}"
            } > release-notes.md
          fi

      - name: Download bundled artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: ${{ env.APP_SLUG }}-*-bundles
          path: release-assets

      - name: Generate updater manifest
        if: env.BUILD_FLAVOR == 'direct'
        env:
          EXPECTED_UPDATER_PLATFORMS: windows-x86_64
          GITHUB_REPOSITORY: ${{ github.repository }}
          RELEASE_ASSETS_ROOT: release-assets
          RELEASE_NOTES_PATH: release-notes.md
          RELEASE_VERSION: ${{ env.RELEASE_TAG }}
        run: |
          set -euo pipefail
          export RELEASE_VERSION="${RELEASE_VERSION#v}"
          node .release-scripts/scripts/release/generate-updater-manifest.mjs

      - name: Sync updater manifest to NexusTools API
        if: env.BUILD_FLAVOR == 'direct'
        env:
          NEXUSTOOLS_API_URL: ${{ env.NEXUSTOOLS_API_URL }}
          NEXUSTOOLS_CONFIG_KEY: ${{ secrets.NEXUSTOOLS_CONFIG_KEY }}
          APP_ID: ${{ env.APP_SLUG }}
          MANIFEST_PATH: release-assets/latest.json
        run: |
          set -euo pipefail
          node .release-scripts/scripts/release/sync-updater-manifest.mjs

      - name: Collect release files
        id: release_files
        shell: bash
        run: |
          set -euo pipefail

          find release-assets -type f ! -name 'release-metadata.json' | sort > release-files.txt

          {
            echo 'paths<<__FILES__'
            cat release-files.txt
            echo '__FILES__'
          } >> "${GITHUB_OUTPUT}"

      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: ${{ env.APP_PRODUCT_NAME }} ${{ env.RELEASE_TAG }}
          body_path: release-notes.md
          draft: ${{ github.event_name == 'workflow_dispatch' && inputs.draft || false }}
          prerelease: ${{ contains(env.RELEASE_TAG, '-') }}
          overwrite_files: true
          fail_on_unmatched_files: true
          files: ${{ steps.release_files.outputs.paths }}
